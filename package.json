{
    "scripts": {
        "postinstall": "npm install --prefix ./src/server && npm install --prefix ./src/server/frontEnd && npm run --prefix ./src/server build",
        "start": "npm start --prefix ./src/server"
    }
}