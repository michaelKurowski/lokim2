{
    "scripts": {
        "postinstall": "npm install --prefix ./src/server && mkdir ./src/server/public && npm install --prefix ./src/server/frontEnd && npm run --prefix ./src/server build",
        "start": "npm start --prefix ./src/server"
    }
}