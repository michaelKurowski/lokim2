const {takeEvery, put} = require('redux-saga/effects')

function* watch() {

}

function* mapActionsToWebSocketEvents(action) {
    switch (action.type) {
        
    }
}

module.exports = {
    watch
}